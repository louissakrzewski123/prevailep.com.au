# Netlify configuration for Prevail Exercise Physiology
# The site is plain static files, so "publish" is the folder itself.

[build]
  publish = "."

# Security & privacy headers (good practice for a health-related site)
#
# app.cal.com appears here for the booking widget, which only runs on
# /booking.html. It's allowed site-wide rather than in a path-scoped block
# because browsers enforce two CSP headers as the *intersection* of both —
# a second, narrower policy for /booking.html would still be blocked by
# this one. No other page references Cal, so nothing else can load it.
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Content-Security-Policy = "default-src 'self'; img-src 'self' data: https://www.googletagmanager.com https://www.google-analytics.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; script-src 'self' https://www.googletagmanager.com https://app.cal.com; frame-src https://app.cal.com https://cal.com; connect-src 'self' https://api.web3forms.com https://app.cal.com https://www.googletagmanager.com https://www.google-analytics.com https://*.google-analytics.com https://analytics.google.com https://*.analytics.google.com; form-action 'self' https://api.web3forms.com; base-uri 'self'; frame-ancestors 'self'"

# Cache fonts/images/styles for speed (HTML stays fresh)
[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=604800"
[[headers]]
  for = "/*.png"
  [headers.values]
    Cache-Control = "public, max-age=2592000"
